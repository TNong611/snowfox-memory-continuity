# 记忆连续性 2.0——SnowFox 的零成本无限聊天框

> 2026-06-27 | 作者：tnong & 雪狐 🦊

---

很多人在使用 AI 助手时都遇到过这个烦恼：

> 聊着聊着，AI 开始"失忆"了。要么忘记你十分钟前说过的话，要么上下文太长烧掉大把 token。

更烦的是——你需要不停地开"新会话"。开新会话 = AI 又不知道你是谁了。

SnowFox 的五级记忆架构解决了这个问题。而且答案出乎意料地简单。

---

## 一、问题在哪？

主流 AI Agent 的输入模式是：

```
[系统提示] + [当前会话的全部历史对话]
```

这有几个坑：

1. **会话越长，token 烧得越多**。你每发一条消息，整个聊天记录都要重新发给模型算一遍。
2. **跨会话就是失忆**。新会话 = 白纸一张，你又得自我介绍一遍。
3. **遗忘全看模型上下文窗口**。到了极限，最前面的对话直接被丢掉，不是逐步遗忘，是整段消失。

---

## 二、SnowFox 的解法

**不传历史，传记忆。**

```
改造前：
[系统提示] + [完整会话历史 × 几百KB]

改造后（SnowFox）：
[系统提示] + [组装好的层级记忆（~50KB）] + [你当前这一条消息]
```

区别是什么？"完整会话历史"变成了"经过整理的精简记忆"。

但这还不够——关键的变化是这句：

### 会话框只用于显示，不作为输入

你在对话框里看到的所有对话记录，**模型看不到**。

那模型怎么"记住"之前的事？靠五级记忆文件：

```
~/AppData/Local/hermes/memories/
├── fixed/         ← F0: 你让AI记住的事（永不丢）
├── recent/        ← L1: 最近几轮完整对话
├── summary/       ← L2: 更早的压缩摘要
├── long_term/     ← L3: 核心长期知识
├── archive/       ← L4: 超旧的归档（可检索）
└── _assembled_context.md  ← 以上全部拼好，给模型读
```

每轮对话结束后，**你的输入 + 我的回复**自动写入 `recent/`。下一轮开始时，系统把这些文件拼起来喂给模型。

---

## 三、这意味着什么？

这意味着**你可以在一个会话框里无限聊下去**。

- 聊了 100 万字的对话历史？不要紧，**模型不读它**。
- 贴了一整个代码仓库到对话框里？**token 消耗不变**。
- 想回到三天前说的某个事？**只要没被压缩掉，就在记忆文件里**。

这是一个**零成本的无限聊天框**——至少对 token 来说是这样。

---

## 四、代价呢？

"免费"的东西总有代价。这里付出的是**细粒度**。

因为记忆文件有容量上限：

| 层级 | 上限 | 满了怎么办 |
|------|------|-----------|
| recent（L1） | 50KB | 最早的 5KB 被压缩成摘要 |
| summary（L2） | 100KB | 最早的 10KB 被进一步压缩 |
| long_term（L3） | 50KB | 最早的 5KB 被归档（不再主动加载） |

被压缩的那部分对话，**不再保留原话**。

所以如果你说了一句特别重要的话，它最多在 L1 保留到被挤出去为止（大约十几轮对话的容量）。之后它变成了摘要的一部分——意思还在，但原话没了。

**跟人类大脑差不多：你七天前早饭吃的什么，大概率不记得了，但你记得你吃过了。**

---

## 五、为什么不用向量数据库？

这是个好问题。

五级记忆用的是**纯文本文件 + 固定容量驱动压缩**。没有向量化、没有语义搜索、没有相似度打分。

理由是：

1. **确定性**：容量超了就压缩，结果可预期。向量检索的结果每次可能不同。
2. **零开销**：不需要嵌入模型，不需要索引构建，不需要 RAG 管道。
3. **可检查**：每个 `.md` 文件都可以直接用记事本打开看。

当然这也意味着 L4 归档只能按文件名和时间搜索。未来的改进方向确实可以用语义检索替代纯文本归档。

---

## 六、F0 固定记忆

F0 是唯一永不压缩的层级。用于：

- **"记住"类**：你告诉 AI "我是某某，学号是xxx" → 写入 `fixed/`，永不丢失
- **"执行"类**：你让 AI "做完某事后提醒我" → 写入 `fixed/`，执行完删除

F0 上限 10KB，手动管理。不自动清理就是为了防丢。

---

## 七、一点总结

SnowFox 的五级记忆系统做了三件事：

| 你关心的问题 | 答案 |
|-------------|------|
| 聊天框堆太大烧 token 吗？ | **不烧**，模型不读聊天框 |
| 需要开新会话吗？ | **不需要**，一个框用一万年 |
| AI 会忘记你是谁吗？ | **不会**，F0 固定记忆 |
| 忘了说过的话怎么办？ | **压缩有损但存在**，L1→L2→L3 逐步降级 |
| 自己能看记忆文件吗？ | **可以**，全是 `.md` 文本文件 |

---

*这个架构已经在 SnowFox（Hermes Agent 的 MIT fork）上实际部署运行。开源后你可以直接拿来用。*
